# Serve HTTPS for the API domain and proxy to the FastAPI service in Docker

# Hermes - API de récupération des données Spotify billions club

hermes.olympe.center {
encode zstd gzip # Enable compression
reverse_proxy olympe-hermes:8000 # Forward to the 'hermes' container's port 8000
}

# Orphée — API de génération de vidéos musicales

orphee.olympe.center {
encode {
	# Pas de compression sur les téléchargements vidéo : le MP4 est déjà
	# compressé (aucun gain), et compression + Range HTTP sont incompatibles
	# (casse le streaming/seek dans le lecteur vidéo).
	match {
		not path /jobs/*/download
	}
	zstd
	gzip
}
reverse_proxy olympe-orphee:8000 # Forward to the 'orphee' container's port 8000
}

# Redirections

artemis-code.olympe.center {
redir https://github.com/olympe-org/artemis permanent
}

elysium.olympe.center {
redir https://spotify-billions.club/ permanent
}

elysium-code.olympe.center {
redir https://github.com/olympe-org/elysium permanent
}

hephaistos.olympe.center {
redir https://vexia.studio/ permanent
}

hephaistos-code.olympe.center {
redir https://github.com/olympe-org/hephaistos permanent
}

ambrosia.olympe.center {
redir https://chromewebstore.google.com/detail/ambrosia/jpabbbaemgkidilifjlfdnpniphihgle?authuser=0&hl=fr permanent
}

ambrosia-code.olympe.center {
redir https://github.com/olympe-org/ambrosia permanent
}

olympe-code.olympe.center {
redir https://github.com/olympe-org/olympe permanent
}